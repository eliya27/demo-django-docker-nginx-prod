version: '3.7'

services:
  app:
    build: 
      context: .
    ports:
       - "6000:6000"
    volumes:
       - ./app:/app

    command: sh -c "python manage.py runserver 0.0.0.0:8000"
    environment: 
       - DEBUG=1        